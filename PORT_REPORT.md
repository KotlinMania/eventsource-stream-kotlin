# Port report — `eventsource-stream-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `eventsource-stream`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 7 `use` sites from 1 other repos
- **Outbound**: 12 `use` sites to 4 other repos
- **Unresolved external**: 3 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `eventsource-stream` | `eventsource_stream` | 7 | 4 | COD-51 | 107 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (7 edges, 4 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `codex-kotlin` | `codex-otel` | 3 | ✅ |
| `codex-kotlin` | `codex-core` | 2 | ✅ |
| `codex-kotlin` | `codex-api` | 1 | ✅ |
| `codex-kotlin` | `codex-client` | 1 | ✅ |

### Symbol-level demand (3 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `eventsource_stream::Event` | 3 | 3 | 0 | 2 |
| `eventsource_stream::EventStreamError` | 2 | 2 | 0 | 2 |
| `eventsource_stream::Eventsource` | 2 | 2 | 0 | 2 |

#### Per-symbol detail (top 3 symbols)

##### `eventsource_stream::Event` (3 sites, 3 codex, 0 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-core` `src/client.rs:88` — `use eventsource_stream::Event`
- `codex-kotlin/codex-otel` `src/events/session_telemetry.rs:43` — `use eventsource_stream::Event as StreamEvent`
- `codex-kotlin/codex-otel` `tests/suite/runtime_summary.rs:10` — `use eventsource_stream::Event as StreamEvent`

##### `eventsource_stream::EventStreamError` (2 sites, 2 codex, 0 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-core` `src/client.rs:89` — `use eventsource_stream::EventStreamError`
- `codex-kotlin/codex-otel` `src/events/session_telemetry.rs:44` — `use eventsource_stream::EventStreamError as StreamError`

##### `eventsource_stream::Eventsource` (2 sites, 2 codex, 0 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-api` `src/sse/responses.rs:12` — `use eventsource_stream::Eventsource`
- `codex-kotlin/codex-client` `src/sse.rs:3` — `use eventsource_stream::Eventsource`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `url-kotlin` | 2 | 1 | `url` | COD-179 | 13 | To Do |
| `http-kotlin` | 2 | 1 | `http` | COD-60 | 18 | To Do |
| `futures-kotlin` | 4 | 2 | `futures` | COD-54 | 32 | To Do |
| `reqwest-kotlin` | 4 | 2 | `reqwest` | COD-119 | 45 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `pin-project-lite` | 118 | 1 | `eventsource-stream` |
| `nom` | 90 | 44 | `eventsource-stream` |
| `futures-core` | 45 | 11 | `eventsource-stream` |
