# Port targets — `eventsource-stream-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `eventsource-stream` that codex-rs uses, and therefore the minimum subset that `eventsource-stream-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-51] (rank `107`, tier `direct`, status `To Do`)

**Cargo package name**: `eventsource-stream`
**Rust module name (in `use` statements)**: `eventsource_stream`

## Summary

- **3** distinct symbols imported by codex-rs
- **7** total `use` statements
- **5** codex source files affected
- **4** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `eventsource_stream::Event`

- **3 `use` site(s)** across **2 codex member crate(s)**: `codex-core`, `codex-otel`
- Aliases used: `as StreamEvent`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/client.rs:88` (`codex-core`) — `use eventsource_stream::Event`
- `otel/src/events/session_telemetry.rs:43` (`codex-otel`) — `use eventsource_stream::Event as StreamEvent`
- `otel/tests/suite/runtime_summary.rs:10` (`codex-otel`) — `use eventsource_stream::Event as StreamEvent`

### `eventsource_stream::EventStreamError`

- **2 `use` site(s)** across **2 codex member crate(s)**: `codex-core`, `codex-otel`
- Aliases used: `as StreamError`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/client.rs:89` (`codex-core`) — `use eventsource_stream::EventStreamError`
- `otel/src/events/session_telemetry.rs:44` (`codex-otel`) — `use eventsource_stream::EventStreamError as StreamError`

### `eventsource_stream::Eventsource`

- **2 `use` site(s)** across **2 codex member crate(s)**: `codex-api`, `codex-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `codex-api/src/sse/responses.rs:12` (`codex-api`) — `use eventsource_stream::Eventsource`
- `codex-client/src/sse.rs:3` (`codex-client`) — `use eventsource_stream::Eventsource`
